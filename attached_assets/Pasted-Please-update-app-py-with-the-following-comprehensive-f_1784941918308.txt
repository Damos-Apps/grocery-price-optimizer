Please update app.py with the following comprehensive feature improvements:1. Database Schema & Variable Size Normalization EngineEnsure the master item database/sheet supports independent columns for each store: Product Name, Price ($P), Quantity ($Q), and Unit Type (e.g., kg, g, L, mL, count).Calculation Formula ($U = P / Q$): Automatically calculate the Normalized Unit Price ($U$) for every product across stores.Dual Output Logic: When comparing items with mismatched sizes (e.g., 1kg at Coles vs. 2kg at ALDI), calculate and display two distinct recommendations:Cheapest Upfront: Out-of-pocket minimum price today.Best Long-Term Value: Lowest unit cost ($/kg, $/L, etc.).2. Update ALDI Routing to DoorDashUpdate the Add to Cart button logic for ALDI items. Direct ALDI links to DoorDash's ALDI marketplace ([https://www.doordash.com/p/aldi-australia](https://www.doordash.com/p/aldi-australia)) for direct home delivery.Set browser link targets so clicking Add to Cart reuses dedicated tabs (target='_coles', target='_woolies', and target='_doordash').3. Interactive 'Quick Price Check' Search Engine (With Live Fallback & Fuzzy Matching)Add an interactive search bar and store selector dropdown (All Stores, Coles Only, Woolworths Only, ALDI Only) inside the 'Quick Price Check' tab.Primary Local Search: Search our internal price database using fuzzy text matching to pair items even if name formats differ slightly. Display side-by-side comparisons showing sizes, unit costs, regular prices, and active sale prices.Fallback Live Search: If a searched item is NOT found locally, display dynamic live search buttons for Coles, Woolworths, and DoorDash/ALDI pre-populated with the typed search query.4. Weekly Specials CSV / Sheet File Upload SystemAdd a 'Weekly Catalogue Upload' section in the app sidebar or top tab.Allow uploading a simple weekly CSV file (or syncing a Weekly_Specials Google Sheet tab) containing promotional sale prices (Product Name, Store, Sale Price).Dynamic Overrides: When calculating shopping lists, if an item on the list appears in the active Weekly Specials file, use the sale price for that week; otherwise, fall back to the saved regular baseline price.